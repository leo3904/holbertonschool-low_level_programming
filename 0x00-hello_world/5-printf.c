#include <stdio.h>

/**
 * main - Start point
 * printf: Print the string
 *
 * Return: Always (0)
 */

int main(void)
{
    printf("with proper grammar, but the outcome is a piece of art,\n");
    return (0);
}
